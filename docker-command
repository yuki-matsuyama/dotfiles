docker ps -a
docker stop $(docker ps -aq)
docker rm $(docker ps -aq)
docker-compose up
docker-compose stop
docker-compose build
docker-compose up php-fpm apache2 memcached mysql
docker-compose build php-fpm apache2 memcached mysql
docker network rm $(docker network ls -q)
docker network ls
docker volume rm $(docker volume ls -q)
docker volume ls
docker
